[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "php82Extensions.mysqli", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.redis", "php82Extensions.mongodb"]

[phases.install]
cmds = ["echo 'Installing PHP and extensions...'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
